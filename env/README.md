# Environment

The conda environment definition for this project is stored in `SPOmiAlign.yml`.

Create the environment with:

```bash
conda env create -f env/SPOmiAlign.yml
conda activate SPOmiAlign
```
