# Resources

This directory collects standalone helper scripts that complement the packaged `SPOmiAlign` modules.

- `align_h5ad.py` writes transformed coordinates back into an h5ad object.
- `reassignment.py` contains a script-oriented reassignment workflow.

The reusable library implementation remains under `../SPOmiAlign/`.
